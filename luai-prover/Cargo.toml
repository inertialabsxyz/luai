[package]
name = "luai-prover"
version = "0.1.0"
edition = "2024"

[dependencies]
luai = { path = "..", features = ["zkvm", "serde", "std"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
