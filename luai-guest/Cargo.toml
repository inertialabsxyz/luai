[package]
name = "luai-guest"
version = "0.1.0"
edition = "2024"

[dependencies]
luai = { path = "..", default-features = false, features = ["zkvm", "serde"] }
serde = { version = "1", features = ["derive"] }

[[bin]]
name = "luai-guest"
path = "src/main.rs"
